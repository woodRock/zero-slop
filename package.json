{
  "name": "zero-slop",
  "version": "1.4.1",
  "description": "ZeroGPT Tweet Checker Chrome Extension",
  "main": "background.js",
  "scripts": {
    "test": "jest",
    "build": "node build.js",
    "build:watch": "node build.js --watch"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-chrome": "^0.8.0",
    "jest-environment-jsdom": "^29.7.0"
  }
}
